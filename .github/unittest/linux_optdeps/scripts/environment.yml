channels:
  - pytorch
  - defaults
dependencies:
  - pip
  - pip:
    - hypothesis
    - future
    - cloudpickle
    - crafter==1.8.3
    - pytest
    - pytest-cov
    - pytest-mock
    - pytest-instafail
    - pytest-rerunfailures
    - pytest-timeout
    - expecttest
    - pybind11[global]
    - pyyaml
    - scipy
    - psutil
    - coverage
    - ray
